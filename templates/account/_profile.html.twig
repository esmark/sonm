{% extends 'account/_layout.html.twig' %}

{% block content %}
    {{ parent() }}

    <h1>Мой профиль</h1>

    <div class="row">
        <div class="col-xl-2">
            {{ knp_menu_render('profile') }}
        </div>

        <div class="col">
            {% block account_content '' %}
        </div>
    </div>

{% endblock %}
